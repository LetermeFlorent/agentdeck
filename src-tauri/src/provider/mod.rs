// Abstraction provider (multi-IA). Claude Code = premier adapter (process persistant,
// entrée/sortie stream-json). D'autres IA pourront ajouter leur module ici.

pub mod claude_code;
pub mod claude_spawn;
pub mod claude_stream;
